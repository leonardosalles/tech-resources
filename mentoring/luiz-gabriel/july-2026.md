### 1. Learning CSS Basics
From July 1 to July 7.

What is a:
- ```selectors``` - id, classes, elements
- ```sub-selectors``` - after, before, last-child, first-child, nth, parent, child
- ```properties``` - color, width, height, position, background, background color
- ```best-practices``` - max selectors, margin, padding
- ```usage``` - difference between style and class property in HTML

Then as action items:
- Create your first HTML page importing a CSS file
- Add a basic style to the page following the proposed image
- Use different selectors to style your page(id, element, class, :after, :before)
- Use at least one parent item as selector to style a child without directly select this child(nested selector)

July 7: ```Lightning Talk``` about CSS, 5 minutes to explain content above.

------------------------------------------------------------------------------------------------------------------------------------------

### 2. Learning Javascript basics
From July 8 to July 15.

What is a:
- ```function``` - also understand function types global versus local and his parameters(method signature)
- ```object``` - how to define a object and add properties inside
- ```event``` - 
- ```object```
- ```usage``` - difference between style and class property in HTML

------------------------------------------------------------------------------------------------------------------------------------------

### 3. Basics wrap-up
From July 16 to July 17.

We gonna use 2 days to refactor and improve our app developed during our basics training. You now should integrate all the CSS and Javascript, as action itens you should:

- Improve app design in general(colors, position, sizes, alignments)
- Add a form with First Name, Last Name and Age with a button to submit the form(should live inside "content" section in image)
- Using a javascript function capture form information and show captured data after submit button
- Find a way to prevent browser default submit behavior(tip: javascript event)

After improvements we should have at least a layout following the same exactly position as the image below: <br>
<img width="680" alt="layout" src="https://github.com/user-attachments/assets/cdfd1a23-0cfe-47db-b542-27cfec9d8537" />

July 17: 15 to 30 minutes ```Presentation``` to show implemented layout, also explain decisions and to answer technical questions.
